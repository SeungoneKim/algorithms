<Tree Dynamic Note>

- 독립집합

- D[N][0]= N인 정점을 독립집합에 포함시키지 않았을 때, 최대 크기
- D[N][1]= N인 정점을 독립집합에 포함시켰을 때, 최대 크기

- 트리에 루트가 없기 때문에, 1을 루트라고 가정하고 문제를 푼다.
- 관계식은,
D[v][0] = sigma(max(D[Wi][0],D[Wi][1]))
D[v][1] = sigma(D[Wi][0])+A[v]
- 관계식을 통해서 V를 루트로 하는 서브트리의 독립집합을 구할 수 있다.