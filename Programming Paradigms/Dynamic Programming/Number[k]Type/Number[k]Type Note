<Number[k] Type Note>

-숫자 k에 대한 유형은 구하라는 값에 따라서 크게 3가지의 유형으로 또 구분할 수 있다.
1. 횟수의 최대,최소값(Max&Min of Trials)
2. 총 방법의 수(Number of Ways)
3. 외부값의 최대,최소값(Max&Min of outer values)

- 각각 다른 방법의 횟수들이 모여서 총 방법의 수를 구성하는 것이다.
- 즉, 여러가지 방법들 중 가장 크고 작은 값을 고르는 것이 1.이고, 그 방법들을 모두 합친 값을 구하는 것이 2.이다.

-숫자 k에 대한 유형은 k가 무엇을 가리키는 것인지에 따라서 또 크게 4가지로 구분할 수 있다.
1. 직사각형에서의 몇번째 행 또는 열(rownum,colnum)
2. 문자열에서의 몇번째 문자(index)
3. 정수 k 숫자(number)
4. n개중에서 k번째 순번(order)

- k가 무엇을 가리키는지는 너무 신경쓰기보다는, 어떠한 특정한 숫자 k번째로 생각하는 것이 편하다.


- memoization에 사용해야하는 배열이 너무 큰 경우 다른 방법을 탐색해야한다.
1. 실제로 사용하는 배열의 크기가 크지 않을때는 map을 이용해서 memoization을 진행한다.
2. 실제로 사용하는 배열의 크기가 클때는 i<=500,0000처럼 특정한 구간만 짤라서 memoization을 진행한다.
-> 수가 클때는 상대적으로 중복 연산이 있어도 이를 감수하는 것이다.