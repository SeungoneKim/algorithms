<Number of Ways Note>

-재귀를 설계할 때 가능한 설계 방법론.
1. set를 전역으로 두고, 조건을 만족할 때 넣으면서 최종적으로 set.size()를 통해 방법의 수 세기.
2. 재귀호출 할때마다 answ=0으로 두고, answ+=go()를 통해 다음 재귀를 호출하며, 최종적으로 1을 리턴하면 상위 재귀에서 하위 재귀의 값을 더해가면서, answ를 리턴
3. 재귀호출 할때마다 answ=dp[][]으로 두고, answ>0이면 바로 리턴(memoization), answ==-1이면 2와 마찬가지의 방법으로 설계
