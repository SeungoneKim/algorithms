<Associate Container Note>

- 연관 컨테이너는 객체를 객체와 연관된 키 값으로 위치를 정한다.
- 키는 기본 타입이 될 수도 있고, 클래스 타입의 객체가 될 수도 있다.
- 객체의 위치는 컨테이너에 지정한 키에 따라서 결정이 되고, 컨테이너 타입 내부의 저장 방식은 STL 구현체에 따라서 다르다.

- 다음과 같은 종류가 있다.
1. map<K,T>
2. multimap<K,T>
3. unordered_map<K,T>
4. unordered_multimap<K,T>
5. set<T>
6. multiset<T>
7. unordered_set<T>
8. unordered_multiset<T>

<map>
- multi 접두어가 붙어 있으면 키가 유일하지 않아도 되고, multi 접두어가 없으면 키가 유일해야 한다.
- unordered_ 접두어가 붙어 있으면, 컨테이너에서의 원소 위치를 키 값 비교가 아니라 키에서 생성한 해시값으로 정한다는 것이다.
- 즉, unordered_ 접두어가 없으면 원소 순서를 키 값 비교로 정한다는 뜻이다.

<set>
- 개별 키를 갖지 않는 다는 점을 제외하면 집합 컨테이너는 map 컨테이너와 매우 비슷하다.
- multi 접두어가 붙어 있다면 객체들이 유일하지 않아도 되고, multi 접두어가 없다면 객체가 유일해야 한다.
- unordered_ 접두어가 붙어 있다면, 해시 값을 사용해서 원소들을 저장하고, 없다면 less<T>로 정렬해서 원소들을 저장한다.
- 순차 컨테이너(ordered)는 순방향과 역방향 반복자를 얻을 수 있지만, 비순차 컨테이너에서는 순방향 반복자만 얻을 수 있다.

- 저장된 원소가 매우 많고 임의로 삽입과 가져오기 연산이 수행될 때는 비순차 집합이 순차집합에서 작업하는 것보다 빠르다.

