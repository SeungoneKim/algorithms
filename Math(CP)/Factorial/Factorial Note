<Factorial Note>

- 팩토리얼은 매우 큰 값이다.
- 실제로 다룰 수 있는 범위는 int범위에서는 12!까지이다.

- 팩토리얼을 이용해서 연산을 하는 문제는 그 값 자체를 구하는 문제는 아니다.
- 대게는 소인수분해를 하여 답을 구할 수 있는 문제들이다.

// code

long long fact[1000001];

fact[0] = 1;
for (int i=1; i<=n; i++) {
    fact[i] = (fact[i-1] * i) % mod;
}